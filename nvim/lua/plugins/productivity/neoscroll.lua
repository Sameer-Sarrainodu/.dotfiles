require("neoscroll").setup({
	-- All these are defaults; customize as needed
	easing_function = "quadratic", -- smooth scroll
	hide_cursor = true,
	stop_eof = true,
	respect_scrolloff = false,
	cursor_scrolls_alone = true,
	pre_hook = nil,
	post_hook = nil,
})
